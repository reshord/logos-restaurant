export type CardInfo = {
    id: number,
    image: string,
    title: string,
    count: number,
    description: string,
    weight: string,
    price: number
}

export type orderProd = {
    id: number,
    image: string,
    title: string,
    count: number,
    description: string,
    weight: string,
    price: number
}
